'use strict';

const CATALOG_NAME = 'catalogo.txt';
const REPLICA_META_NAME = 'replicas.json';
const REPLICA_COUNT = 2;
const MAX_HOPS = 32;

module.exports = {
  CATALOG_NAME,
  REPLICA_META_NAME,
  REPLICA_COUNT,
  MAX_HOPS
};
